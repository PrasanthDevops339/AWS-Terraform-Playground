# terraform-aws-lambda

Reusable Terraform module to package and deploy an AWS Lambda function, optionally upload the package to S3, and wire common options like VPC config, logging, DLQ, file system, image config, event source mappings, and invoke permissions.

## Inputs and Outputs

See variables.tf and outputs.tf for the full contract.
